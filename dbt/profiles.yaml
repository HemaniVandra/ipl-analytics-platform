ipl_databricks:
  target: dev
  outputs:
    dev:
      type: databricks
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"  # SQL warehouse HTTP path
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      schema: /ipl_catalog/gold
      threads: 4